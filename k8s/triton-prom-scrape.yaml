apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: triton-monitor
  namespace: monitoring
spec:
  selector:
    matchLabels:
      app: triton
  namespaceSelector:
    matchNames:
      - aimodel
  endpoints:
    - port: metrics
      interval: 15s
